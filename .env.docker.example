# Docker Environment Configuration for Rowt UI
# Copy this file to .env and update the values

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Your Rowt server API endpoint (REQUIRED)
ROWT_API_ENDPOINT=https://your-rowt-server.com

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================

# Build mode: Controls how the container behaves
# - development: Hot reloading, volume mounting, dev server on port 8080
# - production: Built app served with Nginx on port 80
BUILD_MODE=production
NODE_ENV=production

# ============================================================================
# PORT CONFIGURATION
# ============================================================================

# Host ports (what you access from your machine)
DEV_PORT=8080      # Development mode: http://localhost:8080
PROD_PORT=3000     # Production mode: http://localhost:3000

# Container ports (internal, usually don't change these)
# Development uses container port 8080, Production uses container port 80

# ============================================================================
# TRAEFIK REVERSE PROXY (OPTIONAL)
# ============================================================================

# Enable Traefik labels for reverse proxy
TRAEFIK_ENABLE=false
TRAEFIK_HOST=rowt-ui.localhost

# ============================================================================
# USAGE EXAMPLES
# ============================================================================

# Development:
#   BUILD_MODE=development docker-compose up -d rowt-ui
#   or: make dev
#
# Production:
#   BUILD_MODE=production docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d rowt-ui
#   or: make prod

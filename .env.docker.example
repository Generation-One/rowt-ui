# Docker Environment Configuration for Rowt UI
# Copy this file to .env and update the values

# ============================================================================
# REQUIRED CONFIGURATION - MUST BE SET!
# ============================================================================

# Your Rowt server API endpoint (MANDATORY - NO DEFAULT)
# You MUST set this to your actual server URL
ROWT_API_ENDPOINT=https://your-rowt-server.com

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================

# Build mode: Controls how the container behaves
# - development: Hot reloading, volume mounting, dev server on port 8080
# - production: Built app served with Nginx on port 80
BUILD_MODE=production
NODE_ENV=production

# ============================================================================
# PORT CONFIGURATION
# ============================================================================

# Host ports (what you access from your machine)
HOST_PORT=8080

# ============================================================================
# TRAEFIK REVERSE PROXY (OPTIONAL)
# ============================================================================

# Enable Traefik labels for reverse proxy
TRAEFIK_ENABLE=false
TRAEFIK_HOST=rowt-ui.localhost

# ============================================================================
# USAGE EXAMPLES
# ============================================================================

# Development:
#   BUILD_MODE=development HOST_PORT=8080 CONTAINER_PORT=8080 docker-compose up -d
#
# Production:
#   BUILD_MODE=production HOST_PORT=3000 CONTAINER_PORT=80 docker-compose up -d
#
# Custom port (e.g., 9999):
#   BUILD_MODE=development HOST_PORT=9999 CONTAINER_PORT=8080 docker-compose up -d

# Docker Environment Configuration for Rowt UI
# Copy this file to .env and update the values

# Required: Your Rowt server API endpoint
ROWT_API_ENDPOINT=https://your-rowt-server.com

# Optional: Environment (development or production)
NODE_ENV=production

# Optional: Custom ports for Docker Compose
DEV_PORT=8080
PROD_PORT=3000
